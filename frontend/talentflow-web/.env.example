# TalentFlow AI — Frontend (Vite)
# Copy to .env for local dev. Set in Netlify → Environment variables for production.

# Production (required on Netlify)
VITE_API_BASE_URL=https://YOUR_RENDER_BACKEND_URL.onrender.com/api/v1

# Local development
# VITE_API_BASE_URL=http://localhost:8080/api/v1
